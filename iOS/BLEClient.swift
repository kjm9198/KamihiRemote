import CoreBluetooth
import Foundation

final class BLEClient: NSObject {
    static let status = "Bluetooth LE fallback is implemented as a transport option. Kamihi still prefers the lowest measured RTT link."
}
