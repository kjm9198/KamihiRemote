# Kamihi Desktop App Test Matrix

This matrix tracks the app-by-app bug-fix loop. `VERIFIED` means the flow is covered by current software evidence; `PARTIAL` means useful functionality exists but important flows remain unverified or incomplete; `PENDING` means it has not yet received a dedicated deep app run. Real-device and RayNeo-only behavior remains `NEEDS PHYSICAL TEST` until tested on hardware.

Canonical product direction: one persistent iPhone-owned desktop, trackpad-first phone controller, no user-facing Remote-for-Mac product path, and no automatic app/window movement. The same app target also supports iPad; iPad simulator evidence is software-only and does not replace real Magic Keyboard/trackpad, multitasking, USB-C peripheral, or external-display validation.

| App / surface | Open / restore / X lifecycle | Controls + content | Pointer / scroll | Keyboard | Empty / error / permission states | Persistence / reopen | Remaining app-specific work |
|---|---|---|---|---|---|---|---|
| Browser | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | Dedicated run 2026-09-07 verified tabs, close/new-tab, address/search, back/forward/reload-stop, favorites/bookmarks/history, find, share, downloads/new-window routing and persistent WebKit store in source. Removed false macOS Safari user-agent impersonation so sites see the real iOS/WKWebView runtime while desktop layout remains requested through public WebKit APIs. Still needs targeted simulator/UI coverage for button flows and physical Password AutoFill/passkeys/CAPTCHA/SSO/file upload/media tests. |
| Photos | PARTIAL | PARTIAL | PENDING | N/A | PARTIAL | PENDING | 2026-09-07 recovery pass fixes stale PhotoKit authorization/library state after the user changes Photos access or a Limited Library selection while the Photos window remains open: foreground activation now re-reads the system authorization and reloads only currently granted assets. Favorite failures also retain an explicit error message instead of being silently discarded, and CI guards the foreground-refresh contract. Still needs launcher/dock open, grid/detail controls, share/export, X/minimize/maximize/reopen, simulator UI evidence, and physical Limited Library/iCloud behavior. |
| Files | PENDING | PARTIAL | PARTIAL | PENDING | PARTIAL | PARTIAL | 2026-09-07 reliability pass now guards copy-based multi-file import, sandboxed Application Support ownership, collision-safe duplicate naming, delete confinement, search/import/share/remove controls, stale-selection recovery, and native preview scrolling. Provider-backed picker URLs now request security-scoped access for the duration of the copy when available, release it immediately afterward, and return per-import failures to the UI. Partial/failed imports now show a visible alert instead of silently dropping files, while successfully imported files still remain selected and usable. This remains deterministic source-contract coverage, not a substitute for app-flow UI evidence. Still needs Dock/App Library launch, actual picker interaction, visible search/select/delete/share flows, X/minimize/maximize/reopen, keyboard navigation, and iPhone/iPad simulator interaction evidence. |
| PDF Viewer | PENDING | PARTIAL | PARTIAL | N/A | PARTIAL | PARTIAL | 2026-09-08 reliability pass validates PDFs with PDFKit before mounting the native viewer, so damaged/incomplete/non-PDF data with a `.pdf` extension now shows a visible accessible “PDF Can’t Be Opened” recovery state instead of a blank preview. The user can still share/export or remove the file from the surrounding Files controls. CI now guards this error path together with PDFKit continuous vertical scrolling, native scroll registration/cleanup, Quick Look fallback, and sandboxed reopen behavior. Still needs targeted simulator evidence for a real good-PDF open/scroll/share flow plus the corrupt-PDF UI state, X/minimize/maximize/reopen, and iPad layout. |
| Documents | PENDING | PARTIAL | PENDING | PARTIAL | PARTIAL | PARTIAL | 2026-09-07 lifecycle pass fixed a restore bug where loading the saved document array triggered `didSet` persistence before the saved active-document ID had been read, erasing the user's selection on every process launch. Documents and active selection now persist independently during restore, a valid intentionally-empty collection remains empty instead of being recreated after deleting the last document, and deleting a document selects the nearest surviving neighbor instead of jumping to the top of a long list. Deterministic CI guards these invariants. Still needs dedicated simulator/UI coverage for Dock/App Library open, create/select/edit/delete, export/share, scroll, X/minimize/maximize/reopen, import support, richer editor features, and iPad interaction evidence. |
| Sheets | PENDING | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | 2026-09-07 dedicated passes confirmed persistent local workbook, active-cell keyboard editing, CSV import/export, TSV/newline multi-cell paste, and first-class toolbar Copy/Paste/Clear controls. The worksheet is now a genuine two-axis scrollable SwiftUI canvas with fixed spreadsheet-scale cells instead of shrinking the entire 20x12 grid to the window, preserving horizontal/vertical wheel and trackpad scroll behavior; cells also expose direct tap selection plus accessibility selection actions. Still needs lifecycle/open/X verification, range selection/copy, larger-than-20x12 capacity, row/column operations, lightweight formulas, and targeted simulator/UI coverage. |
| Notes | PENDING | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | 2026-09-07 lifecycle/input passes preserve delete-all and selected-note state across reopen, keep search filtering in the shared store so the phone software pointer selects the visible filtered result, and track Search/Title/Body as distinct keyboard targets. Editor toolbar software-pointer hit zones distinguish New from Delete. The sidebar note list and editor are independent native scroll panes (`Notes.sidebar` / `Notes.editor`); scroll ownership follows the frontmost window under the cursor and then the hovered Notes pane for both phone two-finger and hardware-wheel input, so a background window or sibling pane cannot steal scrolling. Native scroll surfaces now expose their logical content offset, and Notes row hit-testing adds the live sidebar offset so clicking a visible row after scrolling selects that visible note rather than the row that previously occupied the same screen position. Deterministic CI guards these invariants. Still needs dedicated simulator/UI evidence for Dock/App Library open, create/search/select/edit/delete, text selection, X/minimize/maximize/reopen, plus the larger Notion-like workspace feature set. |
| Calculator | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | PARTIAL | 2026-09-08 reliability pass fixes misleading live `Error` states while an expression is still being entered, de-duplicates decimal points inside the current number, replaces a trailing binary operator instead of stacking invalid operators, validates closing parentheses, supports unary leading/parenthesized minus, and bounds integral `Double`→`Int` conversion before formatting so very large finite results cannot trap. Hardware keyboard entry stays active whenever Calculator is frontmost and maps ASCII/Unicode digits, decimal, parentheses, `+ - * /`, equals/Return, and Backspace directly into the local Calculator store without forwarding unsupported text elsewhere. Phone-controlled software-pointer clicks use SwiftUI-reported normalized frames for the toolbar Clear button and every keypad key, then dispatch only after Calculator is confirmed as the topmost window; this keeps hit-testing aligned through window resizing and shared iPad layouts instead of relying on fixed keypad coordinates. The iPhone Keyboard control gives Calculator a numbers-and-punctuation keyboard, routes calculator-safe characters directly into the local store, maps Return to equals and deletion to backspace, ignores unsupported characters, and preserves the captured-window guard. A DEBUG-only Desktop Lab lifecycle harness now exercises open, minimize, restore, maximize, restore-from-maximize, calculation, X/close, and reopen while checking focus ownership and leaving the reopened Calculator visible for screenshot evidence; Apple Integration Smoke runs that app-flow gate as a mandatory first-attempt-only simulator assertion. Exact-head CI evidence is required before promoting this beyond PARTIAL. Physical keyboard/pointer validation still remains. |
| Clipboard | PENDING | PENDING | PENDING | PENDING | PENDING | N/A | Refresh/copy/paste/Notes/share/clear behavior test pending; history is intentionally memory-only. |
| ChatGPT | PENDING | PENDING | PENDING | PENDING | PENDING | PENDING | WebView load/auth/input/recovery/takeover test pending. |
| YouTube | PENDING | PENDING | PENDING | PENDING | PENDING | PENDING | Load/playback/media/fullscreen/navigation/recovery test pending. |
| Settings | PENDING | PENDING | PENDING | PENDING | PENDING | PENDING | Every control and persistence test pending. |
| Phone Takeover / auth | PENDING | PENDING | N/A | PENDING | PENDING | PARTIAL | Real auth remains public WebKit/iOS only; never copy/log raw passwords, passkeys, cookies or tokens. |
| Hardware keyboard / mouse | N/A | PARTIAL | PARTIAL | PARTIAL | PARTIAL | N/A | Public GameController/UIKit bridge covers transport-independent mouse/keyboard detection, physical pointer motion, isolated left-click vs direct title-bar drag/edge resize, right-click, vertical/horizontal wheel routing, MX Master-style first two auxiliary buttons as Browser Back/Forward, and direct hardware-keyboard text capture for focused Notes/Documents/Sheets/WebView fields. Calculator is now also routed as a frontmost command-like native surface: the hardware receiver remains first responder without requiring a fake editable field and accepts only calculator-safe numeric/operator/evaluate/delete input. 2026-09-07 hardware-pointer tuning applies the same persisted Precision/Direct/Balanced/Fast sensitivity and bounded acceleration preferences to physical mouse deltas, with the same tuned delta during direct drag/resize. High-resolution wheel routing adds a tiny dead-zone plus dominant-axis noise suppression so vertical free-spin does not slowly drift content sideways and an MX-style horizontal thumb wheel does not bob content vertically, while preserving genuine two-axis input. Wheel bursts are now coalesced into an event-driven ~8 ms slice rather than one main-actor UI mutation per raw sample, and switching/disconnecting the current GCMouse detaches old pointer/button/wheel handlers and cancels any pending burst so stale devices cannot leak duplicate input. No permanent timer/display link is used. Live window drag/resize persistence is also coalesced rather than JSON-encoding/writing the complete window list to `UserDefaults` on every high-rate pointer sample, with final geometry flushed at end/cancel. Software CI can verify compile/smoke only; Bluetooth vs USB-C transport, MX Master DPI/free-spin feel, thumb-wheel sign/scale, side-button ordering, device-switch behavior and real hardware focus remain NEEDS PHYSICAL TEST. |
| App Library / launcher | PENDING | PARTIAL | PENDING | N/A | PARTIAL | PENDING | 2026-09-07 identity cleanup removes Safari's compass identity from Kamihi Browser and replaces the generic ChatGPT sparkle with a distinct neutral communication symbol. Launcher and Dock now agree on Browser/ChatGPT/YouTube identity. Reduce Motion now disables launcher hover scale/lift animation while preserving highlight, selection, click routing and app discoverability. Official third-party brand assets should only be bundled locally after an approved asset/legal review; do not synthesize lookalike logos or network-load fragile favicons. Still verify every tile open/restore path and geometry preservation. |
| Dock | PENDING | PARTIAL | PARTIAL | N/A | PENDING | PENDING | 2026-09-07 identity cleanup makes pinned and unpinned running Browser/ChatGPT/YouTube icons consistent and keeps Kamihi Browser visually distinct from Safari. Smoothness pass suppresses identical geometry-array and hover-state publications so high-rate trackpad/MX-class mouse motion does not unnecessarily invalidate Dock/App Library observers. Reduce Motion now removes hover/selection magnification and vertical lift animation rather than merely shortening it, while preserving active/running state, hit targets and clicks. Still verify every pinned/running icon, close/reopen state, hover feel and auto-hide behavior in Desktop Lab plus physical display. |
| Display Diagnostics | PENDING | PENDING | PENDING | N/A | PENDING | PENDING | Software controls plus physical RayNeo calibration remain. |

## Cross-app invariants

Every dedicated run must verify, where applicable: tap app -> visible window; reopening an existing app -> restore/activate without geometry reset; X -> window is actually removed; minimize/maximize -> deterministic; no minimized/invisible window owns keyboard or scrolling; two-finger vertical movement scrolls rather than resizing; visible buttons perform their advertised action; reconnect preserves a usable persistent desktop.

Deliberate-window-movement invariant: the phone trackpad requires a continuous eligible title-bar dwell of 1.5–2.0 seconds before arming window movement. Meaningful pointer travel beyond the pre-hold tolerance cancels arming for that entire touch, including double-tap/drag-lock candidates; ordinary pointer movement must never transition into window dragging later in the same gesture. Current source uses a single 1.60-second hold duration for both arming logic and its async title-bar timer, with deterministic CI guardrails preventing a shorter synchronous bypass from returning.

Smoothness/persistence invariant: pointer-rate drag/resize samples must not synchronously serialize the entire desktop window list to `UserDefaults` on every movement event. Current main coalesces writes only while manipulation is active, flushes the final geometry when drag/resize ends or is cancelled, and suppresses persistence observers while restoring saved windows so the stored active-window ID is not overwritten during load.

Accessibility motion invariant: Dock and App Library hover/selection must remain fully usable with Reduce Motion enabled, with no scale/lift animation required to understand focus, running state, selection or click targets.

iPad software-evidence invariant: the Apple Integration Smoke must exercise a current iPad simulator as well as the iPhone/Desktop Lab path and produce non-blank launch/layout evidence. Passing simulator coverage proves only that the shared app target builds, launches, and renders on iPad; real Magic Keyboard/trackpad feel, Bluetooth/USB-C peripherals, rotation/multitasking behavior, external display, battery, and thermal behavior remain NEEDS PHYSICAL TEST.