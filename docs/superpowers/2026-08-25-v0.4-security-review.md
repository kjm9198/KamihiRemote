# Kamihi Remote v0.4 — Manual Security Review

**Branch:** `feat/v0.4-ui-completion` vs `main`  
**Date:** 2026-08-25  
**Reviewer:** Agent (automated security-review subagent unavailable)

## Scope

UI shell refactor only: AppShell, keyboard overlay, presentation/deck/controller screens, gesture engine tuning, touch animation. No new transport, pairing, or crypto protocols.

## Findings

| Severity | Location | Finding |
|----------|----------|---------|
| — | — | **No new critical or high issues identified in this diff.** |

## Notes

- **Keyboard overlay** sends text via existing `RemoteSession.send(.typeText)` / `keyDown` over authenticated session — no new exfil path.
- **Deck Mac app gallery** reads `hostApps` from existing TCP catalog; no arbitrary bundle-ID injection beyond user-selected tiles.
- **Gesture / scroll changes** are client-side only; Mac `InputEngine` hotkey fallbacks use same synthetic event path with existing accessibility gate.
- **DEBUG self-checks** gated behind `#if DEBUG` — prevents production abort; sim-only preconditions remain debug-only.
- **Pre-existing:** UDP realtime may use pairing-token V1 when K3 session unavailable (documented in v0.3 spec); unchanged by this branch.

## Recommendation

Safe to merge from a UI-layer perspective after physical regression. Re-run full security-review subagent when quota available for second opinion on unchanged crypto/session code.
