name: Apple Build

on:
  pull_request:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

# Every commit keeps its own first-attempt evidence. A newer push must never
# cancel validation for an older SHA; readiness triage compares exact heads.
concurrency:
  group: apple-build-${{ github.workflow }}-${{ github.sha }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: Xcode version
        run: xcodebuild -version

      - name: Build Kamihi Desktop for iOS Simulator
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/kamihi-apple-build"
          xcodebuild \
            -project KamihiRemote.xcodeproj \
            -scheme KamihiRemote \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            ENABLE_PREVIEWS=NO \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | tee "$RUNNER_TEMP/kamihi-apple-build/ios-build.log"

      - name: Upload Apple build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: apple-build-${{ github.sha }}-${{ github.run_attempt }}
          if-no-files-found: ignore
          retention-days: 7
          path: |
            ${{ runner.temp }}/kamihi-apple-build/*.log
